import React, { Component } from 'react';
// import BMR from './Bmr';

function RecGood(props) {
  // let { data } = this.props;
  // console.log(data);
  return (
    <>
      <h1>{props.calories}</h1>
    </>
  );
}

export default RecGood;
